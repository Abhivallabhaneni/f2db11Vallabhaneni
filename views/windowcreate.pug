extends layout 
 
block content 
  h1= title 
  
  form(id='windowForm', action="/resource/window", method="POST" ,class='d') 
    div(class='d').form-group
    div(class='d e').form-group 
        label.windowAttr(for='window_type') window window_type
        input#window_type(type='text', name='window_type') 
    div(class='d e').form-group 
        label.windowAttr(for='size') window size 
        input#size(type='text', name='size') 
    div(class='d e').form-group 
        label.windowAttr(for='cost') window cost 
        input#cost(type='number', name='cost') 
    input(type="submit") 
    
 
  script. 
    document.forms['windowForm'].addEventListener('submit', (event) => { 
      event.preventDefault(); 
      // TODO do something here to show user that form is being submitted 
      fetch(event.target.action, { 
        method: 'POST', 
        body: new URLSearchParams(new FormData(event.target)) // event.target is the form 
      }).then((resp) => { 
        return resp.json(); // or resp.text() or whatever the server sends 
      }).then((body) => { 
        alert("Save succeded") 
        // TODO handle body 
      }).catch((error) => { 
        // TODO handle error 
        alert("Save failed " + error ) 
      }); 
    }); 